<h1>Leagues</h1>

<% if @leagues.count == 0 %>
  <h2>Sorry, no leagues here yet.</h2>
<% else %>
  <ul>
    <% @leagues.each do |league| %>
      <li>
        <%= link_to league.name, league %>
         -
        <%= league.clubs.count %>
      </li>
    <% end %>
  </ul>
<% end %>